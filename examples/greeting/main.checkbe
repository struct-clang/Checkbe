import Bridge
import Greeting

body Application {
    let capability const = new Capability(CAP_READ);

    func main() {
        Bridge.print("What is your name? ")
        let:const string myString = Bridge.readln()
        Bridge.println(Greeting.greet(myString))
    }
}
